# 工作日志

> **规矩**：每次收工在**最上面**加一条。写清楚你是谁、改了什么、自检结果。
> 下一个接手的人靠这个知道现在到哪了。格式照抄下面任意一条。

---

## 2026-09-07 · Claude (shune-aa) · 网屏整张缓存，最坏帧 10.16→3.03ms

**背景**：接手 Codex 的纸张与文字两版后跑自检，**14 / 15，最坏帧 10.16ms 超预算**。
Codex 那边测得 5.49ms —— 同一份代码在不同机器/负载下结果不同，所以这条阈值本身是对的，
不要因为我这边过了就放过。

**定位**（逐项关掉再测）：
- 全开（细网点 + 5 重复 + 纸纹）11.90ms
- 关掉纸纹 8.06ms → 纸纹占 3.8ms（已经有 paperCache，成本是两次全画布 multiply，合理）
- 改成粗网点 1.64ms → **细网点占约 8ms，这是大头，是我的代码**

**改了什么**
- 细网（3px）时一个器物外接框里有一万多个点，×5 重复 ×2 侧 = 每帧十几万次 。
- 网屏本来就锚在纸坐标系、全画面共用一张 —— 那它就该整张缓存。
  新增 ：把整张网渲一次进离屏 canvas，
  平色那一档直接  贴上去，剪影靠已经设好的 clip 裁出来。
  同样的 ox/oy、同样的 hash2、同样的 inkR，**逐像素结果相同**。
  渐变那一档点的大小随位置变，仍然走循环。
- 缓存键含 col / pitch / inv / W / H / 套印偏移，上限 24 张。

**验证**
- 自检 **15 / 15 全部通过**（1080ms），最坏帧 **3.03ms**（原 10.16ms，快 3.4×）
- 目视确认：三个梅瓶细网点跨切线反色，**接缝处网点相位连续**，纸坐标锚定没被破坏

**给下一个人**：Codex 建议的上传图分层错位仍然空着，可以直接开工。
另外注意：仓库里的文件是 **CRLF**，用字符串替换写补丁时要先归一化成 
 再改回去，
否则匹配不上（我踩了一次）。

---

## 2026-09-06 · Codex · 六类纸张与吸墨模型

**改了什么**
- 增加道林纸、新闻纸、牛皮纸、和纸、再生纸与光面纸；每类纸有独立纸色、
  高频颗粒、低频成形、纤维/杂点、网点边缘扰动和套印偏移。
- 纸纹移到纸底与墨层之间，实地墨会覆盖纸纹，文字始终保持锐利；高清导出使用同一渲染管线。
- 覆盖率不再固定为 10–88%，而是由当前纸张决定；滑块、自动配色、候选生成、配方与自检同步更新。
- 候选生成会按纸色实际计算最有对比的兜底墨，避免牛皮纸等中明度纸张上的低对比方案。

**验证**
- 脚本语法通过；自检 15 / 15 全部通过（589ms），最坏帧 5.49ms。
- Playwright 桌面 1440×960 与移动 430×932 均无控制台错误、无横向溢出。
- 六类纸逐一渲染检查；文字编辑、六方案、候选选择和三倍 PNG 下载均通过。

**下一个人可以直接开工的**：上传图的分层错位表现。只给主元素加层次，
不要把全部元素挤出成立体字，也不要让装饰效果破坏当前的纸张与网屏模型。

---

## 2026-09-06 · Codex · 多行文字系统 + 下载闭环 + 窄屏适配

**改了什么**
- 固定标题重构为 `S.texts[]`：每行文字可添加、选择、编辑、删除和拖动，
  并可独立设置横/竖排、四类中英文字体及三档字号。
- 文字参与候选差异计算、历史状态、配方输出和边距约束；旧候选状态会自动迁移。
- 新增三倍尺寸 PNG 下载，导出时不含选择框和界面。
- 新增 820px 以下的单列布局：画布缩放、候选横向滑动、面板不再横向裁切。
- 自检中的标题边距检查扩展为全部文字，并覆盖文字的加/选/改/删。

**验证**
- 脚本语法通过；自检 15 / 15 全部通过（614ms），最坏帧 6.71ms。
- Playwright 桌面 1440×960 与移动 430×932 均无控制台错误、无横向溢出。
- 换展览、添加并编辑英文、切换横排与字体、生成六方案、高清 PNG 下载均通过。

**下一个人可以直接开工的**：纸张材质系统。不要把材质做成覆在画面上的滤镜；
它应改变纸色、纤维/颗粒尺度和吸墨表现，并且仍由同一个 `draw()` 输出到高清下载。

---

## 2026-09-06 · Codex · 独立协作工作区 + 实际页面复核

**改了什么**
- 没有改海报功能；从 `origin/main@c89281d` 建立独立协作分支 `poster-collab`，
  工作区为 `E:\PHD_27\portfolio_poster_collab`，避免主网站工作区的 CV 冲突影响提交。
- 用 Playwright 实际打开页面，检查桌面和 430px 视口；换展览、生成六个方案均可用，
  Canvas 有有效像素输出，控制台和页面脚本均无错误。
- 发现移动端目前仍是桌面工作台横向溢出，不属于已完成的响应式版本；保留为后续任务。

**自检** 15 / 15 全部通过（243ms）

**下一个人可以直接开工的**：文字系统。开工前先读 `poster/AGENTS.md`，只在
`poster-collab` 分支提交；不要碰主工作区的 CV、首页、图片或视频。

---

## 2026-09-06 · Claude (shune-aa) · 建立协作结构 + 多元素

**改了什么**
- 状态从「只能放一个图形」重构成**元素数组** `S.els[]` + `S.si`（选中索引）。
  `fill` / `rep` 搬到每个元素身上；`dot`（网屏频率）仍全局——一张海报只能有一个网屏。
  新增 `addEl` / `delEl` / `hitEl`（从最上面找起）/ Tab 轮选 / Delete 删除。
- **旋转吸附** `snapAngle`：0 / ±90 / 180 + 切线角及其正交，容差 5°。
- **剪刀光标**：没切之前光标是剪刀，切完变默认。
- **画幅**：3:4 / A·B(1:√2) / 方 / 竖屏。换画幅时切线按新比例重新吸附。
- 随机生成约 40% 出两个元素（主形 + 明显更小的配角）。
- 新增 **`?selftest=1` 自检**：15 项不变量，页面上直接出报告。
- 把原型放进 `poster/`，接上 GitHub Pages。
- `.gitignore` 挡掉 `genai-ideation-study/`（**1.1GB**，任何人一个 `git add .` 就会炸）。

**自检** 15 / 15 全部通过（775ms）
- 颜色往返误差 0 ｜ 半调混色对上印刷物理 ｜ 等值线面积误差 0.006%
- 旋成体正侧视与剖面差 0.23% ｜ 192 版 0 违规 ｜ 候选可分辨 1.23 ｜ 最坏帧 7.96ms

**下一个人可以直接开工的**：文字系统（见 AGENTS.md §五 第 1 条，最大一块）。

---
