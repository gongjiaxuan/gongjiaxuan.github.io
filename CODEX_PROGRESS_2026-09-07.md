# CUT / PRINT 本地改进进度交接（2026-09-07）

## 1. 项目位置与状态

- 仓库：`E:\PHD_27\portfolio_poster_collab`
- 当前分支：`poster-collab`
- 核心应用：`E:\PHD_27\portfolio_poster_collab\poster\index.html`
- 本地预览：直接用浏览器打开 `poster\index.html`
- 当前只修改本地文件，**尚未部署或推送 GitHub**。
- `poster/index.html` 是单文件应用，HTML、CSS、Canvas 绘制与交互逻辑都在该文件中。
- 仓库原有 `CODEX_HANDOFF.md` 可作为历史背景；本文件记录 2026-09-07 的最新实现状态，优先级更高。

## 2. 当前产品方向

产品定位是一个具有双色印刷约束的实验海报工作室，而不是一句话自动生成成品的普通 AI 海报工具。

当前主线：

1. 用户从空白画布开始。
2. 用户可以自由选择先切画面、加图形、调印刷或加文字，不要求按固定顺序操作。
3. 展览文案作为可选择的“文字材料”存在，只有用户点击后才进入画布。
4. 生成系统提供材料与变奏，最终构图、取舍和位置由用户决定。
5. 后续 AI 定位应是“材料提供者与变奏伙伴”，而不是替用户完成作品的作者。

## 3. 此前已经完成的基础改进

- 首次打开时画布完全空白，不自动生成示例海报。
- 工具面板改为手动选择的 `切 / 形 / 印 / 字` 四个标签。
- 在“字”面板调整文字后，拖动画布中的图案不会自动跳回“形”面板。
- 图形与文字无需先画切线即可直接添加和拖动。
- 展览命题池已替换为 14 组原创中英双语文学化文案。
- 中英文切换会同步更新仍与展览素材关联、且未被用户手动改写的标题。
- 清空画布会保留当前展览和语言，不会产生中英文状态错位。
- 六个候选方案从空白也能生成，选择后候选层自动收起。

## 4. 本轮已经写入代码的功能

### 4.1 画布内删除

- 在 `.sheet` 内新增 `#selectionDelete`。
- 点击画布中的图形或文字后，选区右上方出现圆形 `×`。
- 删除按钮会根据文字或图形更新无障碍标签。
- 删除图形和删除文字都走原有状态模型，不会切换当前工具面板。

### 4.2 撤销与清空语义分离

- 顶栏新增 `#topUndo`（`↶`）。
- 原来容易被误解为撤销的 `↺` 已改为文字按钮“清空”。
- 历史状态保留在 `HIST` 中，但不再显示画布下方缺乏说明的小缩略图。
- 删除前、添加元素前、添加文字前、切换展览或语言前、选择候选方案前都会保存状态。
- 撤销恢复状态时不会改变当前工具标签。

### 4.3 展览文案成为可点击素材

- `syncBrief()` 已改写为三条可点击素材：
  - `brief:title`：展览标题
  - `brief:body`：文学化正文
  - `brief:meta`：日期与地点
- 每条右侧显示 `＋`；加入画面后显示 `✓`。
- 再次点击已加入的素材不会重复添加，而是选中画面中的对应文字。
- 删除画面文字后，素材行自动恢复 `＋`。
- 用户手动编辑文字后，文字与原始素材解除绑定，后续切换展览或语言不会覆盖用户内容。

### 4.4 “文字种子”交互原型

- “字”面板新增：
  - `#seedInput`
  - `#seedGenerate`
  - `#poemMaterials`
- 输入姓名或短句后可生成四句原创文字草稿。
- 每句都可以单独点击加入画布；重复点击只会选中，不会复制。
- 当前使用本地确定性模板验证交互流程，**没有伪装成已经接入在线 AI**。
- 后续可在不改变界面的情况下，将 `makePoemLines()` 替换为安全的服务端模型接口。

## 5. 最新自动化验证

Playwright 已验证以下流程全部通过：

- 初始画布为空。
- 点击展览标题只添加一次。
- 文案加入后显示 `✓`，画布出现删除按钮。
- 删除文案后素材状态恢复，撤销可恢复文本。
- 标题、正文、时间地点可分别加入。
- 输入“珈萱”可生成四句材料并加入画布。
- 在“字”面板选择、删除图形后仍停留在“字”面板。
- 撤销图形删除后仍停留在“字”面板。
- 测试过程中无 JavaScript 页面错误。

上一轮内置自检结果为 `15/15`。本轮新增代码后仍需在封板前重新运行一次内置自检和移动端完整测试。

测试截图暂存于：

`C:\Users\shune\AppData\Local\Temp\poster-materials-v2-2026-09-07`

## 6. 尚未完成与下一步

按优先级：

1. 重新运行 `?selftest=1`，确认内置规则仍为 `15/15`。
2. 对 390px 手机宽度验证新增撤销、清空、文字素材、诗句素材和选区删除按钮。
3. 进行桌面端视觉检查，重点看删除按钮是否遮挡缩放旋转把手。
4. 检查撤销在连续拖动、连续改色、连续输入文字时的粒度是否自然。
5. 根据视觉结果微调素材行、选区按钮和顶栏尺寸。
6. 用户确认本地效果之前不要部署 GitHub。

## 7. 需要谨慎保留的交互原则

- 工具标签只能由用户手动切换，画布交互不得自动切换标签。
- 点击素材是“加入或选中”，不能再次点击就删除，避免误操作。
- 删除通过画布内 `×` 或键盘 Delete 完成，并允许撤销。
- 任何生成结果先进入素材架或候选页，不能自动覆盖当前作品。
- 不要把 API key 写进前端。真实 AI 需要服务端或 serverless 代理。
- 三维功能若继续做，应优先发展“旋转器物并压印二维投影”，不要加入与印刷概念无关的炫技立体字。

## 8. 给接手 Agent 的最短指令

> 请先阅读 `E:\PHD_27\portfolio_poster_collab\CODEX_PROGRESS_2026-09-07.md`，然后查看该仓库的 `git diff` 和 `poster\index.html`。继续完成第 6 节的验证与修复，只在本地修改，不要部署或推送。不要让画布操作自动切换 `切 / 形 / 印 / 字` 标签，也不要把本地诗句模板声称为真实 AI。

